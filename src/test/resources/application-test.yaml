spring:
  application:
    name: proyecto-backend-test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  mail:
    host: localhost
    port: 25
    username: test@contactocero.app
    password: testpassword

jwt:
  secret: SuperSecretRickPrimeTestKeyForOmniverseDimensionC137OmegaDevice999999
  access-token-expiration-ms: 3600000
  refresh-token-expiration-ms: 86400000

server:
  port: 0
